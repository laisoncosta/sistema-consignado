import { createDashboardStubPage } from "@/components/dashboard/DashboardStubPage";

export default createDashboardStubPage({
  tituloArea: "Área do Supervisor",
  pathname: "/dashboard/supervisor",
});
